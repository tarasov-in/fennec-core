/**
 * Re-export UIProvider and hooks for imports from 'adapters/UIProvider'
 * Used by Field, Model, Collection renderers
 */
export {
  UIProvider,
  useUIOptional,
  useUIAdapter
} from './UIContext'
